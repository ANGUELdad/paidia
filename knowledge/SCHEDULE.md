# Schedule

- Blocks: morning / afternoon / evening with default times
- Day + week matrix; validation: double_book, house_gap
- Override: `force` + `overrideReason` required on conflict
- Template append via `asTemplate` (admin)
- Cancel: `POST /api/schedule/cancel`
