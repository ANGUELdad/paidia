[project]
name = "paidia"
version = "1.0.0"
description = "Armonia Thassos / PAIDIA"
requires-python = ">=3.11"
dependencies = [
  "flask==3.1.1",
  "webauthn==3.0.0",
]

[tool.vercel]
entrypoint = "api.index:app"
