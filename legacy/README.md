# Legacy freeze

Root `index.html` / `app.js` / `server.py` remain the live v69 oracle until cutover.

When `docs/PLATFORM_PARITY.md` is green, move those entrypoints here as read-only reference.

See `/LEGACY.md`.
