# Κατάσταση έργου vs. προδιαγραφή

Ημερομηνία: 2026-08-03. Πηγές: `docs/spec-part1.txt`, `docs/spec-part2.txt`,
`docs/dienstplan.txt`, `docs/nachmittagsbetreuung.txt`.

## Το βασικό θέμα

Η προδιαγραφή περιγράφει **Next.js + TypeScript + PostgreSQL + Prisma στο Vercel**,
με ξεχωριστό media server (MinIO/S3), Redis, background workers για email και AI parsing,
RBAC, validation engine, drag-and-drop, undo/redo και πλήρες audit.

Αυτό που υπάρχει σήμερα (`index.html`) είναι **ένα αρχείο vanilla JS με localStorage**.
Είναι καλό prototype — έχει επικυρώσει ροές με πραγματικά δεδομένα προσωπικού — αλλά
**δεν είναι το σύστημα της προδιαγραφής** και δεν κλιμακώνεται σε αυτό.

Απαιτείται απόφαση πριν συνεχίσουμε να προσθέτουμε features. Δες «Τι προτείνω».

## Τι καλύπτεται ήδη

| Απαίτηση | Πού | Κατάσταση |
|---|---|---|
| Profile picker + PIN (§4.1) | login panel | ✅ |
| Καταγραφή IP / device / user agent στο login (§4.1) | `session`, `logEntry` | ✅ (client-side) |
| Super admin: Zoi, Angelos, Dimi (§4.3.1) | `admin:true` | ✅ μερικώς — μόνο ο κανόνας «μόνιμη αλλαγή προτύπου» |
| Ένας φροντιστής → πολλά παιδιά (§1) | `childIds[]` | ✅ |
| Δύο χώροι, επεκτάσιμο (§3) | `houses[]` | ✅ |
| Saved groups, π.χ. Zwillinge (§5.3) | `groups[]` | ✅ βασικό |
| Εβδομαδιαίο πρόγραμμα σαν το έντυπο (§7.1) | Tag/Woche views | ✅ |
| Clickable κελιά + presets + νέο preset (§31.1) | `sheetEntry`, `＋ Neue Aktivität` | ✅ |
| Ενιαίο inventory λίστας + ψυγείου (§10.1) | `listEntries` + `stock` | ✅ βασικό |
| Ροή Παρασκευής με «δεν υπήρχε» (§11.1, §11.4) | `startFridayBatch` / `confirmFridayBatch` | ✅ |
| Μη διαθέσιμο = μένει ως έλλειψη (§11.4) | status `missing` + carry-over | ✅ |
| Παραλαβή → αυτόματα στο stock (§11.1.10) | `confirmFridayBatch` | ✅ |
| Append-only audit (§12.3) | `DB.log` | ✅ βασικό |
| «Ποιος έκανε τι», ορατό σε όλους (§36.2) | κάρτα στο Βιβλίο | ✅ βασικό |
| Δίγλωσσο UI | DE/EL switch | ✅ (εκτός προδιαγραφής, δικό σου αίτημα) |

## Τι λείπει — κατά προτεραιότητα

### Δεν έχει ξεκινήσει καθόλου

1. **Δύο ξεχωριστές είσοδοι: Προσωπικό / Παιδιά (§31.3)** και **Child Portal (§31.4)**.
   Το ζήτησες δύο φορές. Χρειάζεται δικό του PIN ανά παιδί και εντελώς χωριστό dashboard
   (τι κάνω σήμερα, events εβδομάδας, ανακοινώσεις) χωρίς πρόσβαση σε δεδομένα άλλων.
2. **Βάρδιες, incl. 24ωρες (§6)**. Έχω πλέον τα πραγματικά δεδομένα στο
   `docs/dienstplan.txt` — π.χ. Löhri Τρίτη 11:00 → Τετάρτη 11:00 (24h), Claudio
   Κυριακή 11:00 → Δευτέρα 11:00. Το τρέχον μοντέλο έχει μόνο μπλοκ 10–14/15–19/19–22,
   που **δεν χωράει** 24ωρη βάρδια.
3. **Validation engine (§9, §35)**. Ο πυρήνας του «να αποφευχθούν λογικά λάθη»:
   ίδιο άτομο σε δύο σπίτια ταυτόχρονα, επικαλυπτόμενες βάρδιες, παιδί σε δύο ομάδες,
   σπίτι χωρίς προσωπικό, νυχτερινή διαμονή χωρίς κάλυψη, duplicate detection.
   Warnings με **override + αιτιολογία**, όχι απαγόρευση.
4. **Ημερήσιο πρόγραμμα / daily meeting board (§8)**. Το εβδομαδιαίο είναι Grundplanung·
   η τελική κατανομή γίνεται καθημερινά στις 13:30. Λείπει εντελώς η οθόνη.
5. **Email reminders Κυριακής (§13)** + escalation.
6. **Shift handover + κανόνας 10 λεπτών (§6.3, §6.4)**.

### Απαιτούν backend — αδύνατα σε στατικό PWA

7. **Αποστολή email / WhatsApp (§13, §20)**. Ένα στατικό PWA **δεν μπορεί** να στείλει
   email ή WhatsApp. Χρειάζεται server job (Resend/SES για email, WhatsApp Business API
   ή Twilio). Το μόνο που γίνεται σήμερα client-side είναι `mailto:` και `wa.me` links
   που ανοίγουν την εφαρμογή του χρήστη — χρήσιμο για demo, όχι για αυτόματες υπενθυμίσεις.
8. **Server-side IP & timestamp**. Ό,τι στέλνει ο client πλαστογραφείται. Για ένα σύστημα
   που υπάρχει ακριβώς για να λύσει αμφισβητήσεις κλοπής, αυτό είναι κρίσιμο.
9. **Hashed PIN, lockout μετά από αποτυχίες (§4.2)**. Τώρα τα PIN είναι plain text στον
   κώδικα — αποδεκτό μόνο για demo.
10. **Φωτογραφίες σε ιδιόκτητο server με signed URLs (§14)**. Τώρα base64 σε localStorage,
    που θα γεμίσει σε λίγες δεκάδες φωτογραφίες.
11. **AI parsing λιστών (§11.5, §24)** μέσω Claude API.

### Σημαντικά αλλά δεύτερης φάσης

12. Drag-and-drop παντού + undo/redo (§32) — η προδιαγραφή το θέτει ως **κύριο** τρόπο χρήσης.
13. Πλήρες RBAC με 4 ρόλους (§4.3) — τώρα υπάρχει μόνο admin/μη-admin.
14. Statuses λίστας: Partially Purchased, Replaced, Cancelled (§11.2) — τώρα μόνο 4.
15. Λόγος αφαίρεσης από ψυγείο με presets (§10.4, §31.1) — τώρα δεν ζητείται λόγος.
16. Απογραφή + discrepancy report (§10.6).
17. Reports (§21), backups (§23), GDPR/retention (§22).

## Δεδομένα που προέκυψαν από τα PDF

- **Παιδιά (§5.1):** Simon, Kai, Vincent, Klein Julian, Groß Julian, Lea, Valeria, Jule,
  **Zwillinge** (δύο ξεχωριστά records + saved group), Daniel, Leonie (προσωρινά).
  Η τρέχουσα λίστα στο demo είναι ελλιπής — λείπουν Jule, Daniel, Leonie, τα δίδυμα.
- **Προσωπικό:** Zoi, Angelos, Dimi, Dora, Karin, Claudio, Löhri, Amalia. ✅ σωστό.
- **Κανόνας:** Amalia έχει σχεδόν πάντα τουλάχιστον ένα παιδί (συνήθως ο μικρός Julian).
- **Κανόνας:** Dimi και Angelo = *flexible support*, όχι σταθερή ομάδα.
- **Κανόνας:** η τελική κατανομή αποφασίζεται καθημερινά στις 13:30.
- **Κανόνας 10 λεπτών:** άφιξη 10' πριν· αν η παράδοση δεν ολοκληρωθεί, η προηγούμενη
  βάρδια παραμένει· η καθυστέρηση συμψηφίζεται σε επόμενη μέρα.

## Τι προτείνω

Το prototype έχει κάνει τη δουλειά του: επικύρωσε τη δομή του εντύπου, τη ροή ψωνιών,
τα δικαιώματα και το audit, με πραγματικά ονόματα και πραγματικές βάρδιες.

Από εδώ και πέρα προτείνω **να ξεκινήσει το κανονικό project** όπως το λέει η προδιαγραφή
(Next.js + Postgres + Prisma), με σειρά υλοποίησης §28, και το `index.html` να μείνει ως
UX reference. Ο λόγος: βάρδιες, validation engine, RBAC, email jobs και server-side audit
δεν γίνονται σωστά σε localStorage — και είναι ακριβώς τα σημεία που κρίνουν αν το σύστημα
λύνει το πρόβλημα των αμφισβητήσεων.

Εναλλακτικά, αν θέλεις κάτι δοκιμάσιμο στα χέρια του προσωπικού **αυτή την εβδομάδα**,
συνεχίζω στο prototype με σειρά: (1) δύο είσοδοι + child portal, (2) βάρδιες 24h,
(3) validation warnings. Τα email/WhatsApp μένουν εκκρεμή μέχρι να υπάρχει backend.
